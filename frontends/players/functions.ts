const fn = {
    "makeFilters": function (fio: string, location: string, post: string, array: Array<Object>) {
        console.log('=fio=', fio)
        console.log('=location=', location)
        console.log('=post=', post)
        let arrayAll = [];

        // if (fio) {
        //     arrayAll = array.filter((item: object) => {
        //         if (item.name.toLocaleLowerCase().trim().includes(fio))
        //             return true
        //         // allUsers = users.filter((item) => {
        //         //   if (item.name.toLocaleLowerCase().trim().includes(value)) {
        //         //     return true;
        //         //   }
        //         // })
        //     })
        //     console.log('=c92ed6=', arrayAll)
        //     this.init();
        //     return arrayAll;
        // }

        // if(name && location){}

        // if(name && location && post){}


    }
}

export { fn }