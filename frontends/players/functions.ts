export const makeFilters = function (fio: string, location: string, post: string, array: Array<Object>) {
    console.log('=fio=', fio)
    console.log('=location=', location)
    console.log('=post=', post)
    let arrayAll = [];
    return
}