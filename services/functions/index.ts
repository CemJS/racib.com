export * from './sendApi'

export const loader = async function (Variable) {
  return
}

export const clickSoon = async function (e: MouseEvent) {
  this.Fn.initOne({
    name: "modalSoon"
  })
  e.preventDefault();
  return
}